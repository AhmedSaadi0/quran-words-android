package io.github.ahmedsaadi0.quranwords.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import io.github.ahmedsaadi0.quranwords.R

/**
 * Amiri Quran (OFL) typeface for the Mushaf page reader (D1). Applied only
 * inside ui/mushaf components — the rest of the app keeps FontFamily.Default.
 */
val AmiriQuran = FontFamily(Font(R.font.amiri_quran))
